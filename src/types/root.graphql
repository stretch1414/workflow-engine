type Author {
  name: String!
  books: [Book!]!
}

type Book {
  title: String!
  author: String!
}

type Query {
  authors: [Author]
  books: [Book]
}

type Mutation {
  executePython(input: ExecutePythonInput!): String
}

input ExecutePythonInput {
  functionName: String!
}
